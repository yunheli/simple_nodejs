# This references the default nodejs container from
# the Docker Hub: https://registry.hub.docker.com/_/node/
# If you want Nodesource's container you would reference nodesource/node
# Read more about containers on our dev center
# http://devcenter.wercker.com/docs/containers/index.html
box: node
# This is the build pipeline. Pipelines are the core of wercker
# Read more about pipelines on our dev center
# http://devcenter.wercker.com/docs/pipelines/index.html

# You can also use services such as databases. Read more on our dev center:
# http://devcenter.wercker.com/docs/services/index.html
services:
    # http://devcenter.wercker.com/docs/services/postgresql.html
    - mongo
    - id: mysql
      username: root
      password: root

    # http://devcenter.wercker.com/docs/services/mongodb.html
build:
  # The steps that will be executed on build
  # Steps make up the actions in your pipeline
  # Read more about steps on our dev center:
  # http://devcenter.wercker.com/docs/steps/index.html
  steps:
    # A step that executes `npm install` command 
    - npm-install
    - script:
      name: "A"
      code: |
        export A=1
        export B=1
        export C=1
        export D=1
    - script:
      name: "mongo env"
      code: |
        env | grep mongo
    - script:
      name: "redis env"
      code: |
        env | grep redis
    - script:
      name: "rabbitmq env"
      code: |
        env | grep rabbitmq          
    - script:
      name: "mysql env"
      code: |
        env | grep mysql                              
    - script:
      name: "B"
      code: |
        echo $A
        echo $B
        env
        echo "npm install finish 1"
        echo "AAA" > $WERCKER_REPORT_ARTIFACTS_DIR/a.log  
        echo "AAA" > /tmp/a.log  
  after-steps:
    - script:
      name: "after-steps2"
      code: |
        ls $WERCKER_REPORT_ARTIFACTS_DIR
        echo $A
        env
        ls /tmp

test:
  steps: 
    - npm-test
  after-steps:
    - script:
      name: "test finish"
      code: |
        echo "test finish 1"
        ls $WERCKER_REPORT_ARTIFACTS_DIR
        echo "===================: test start"
        echo $A
        echo $B
        echo "===================: test end"

deploy_production:
  steps:
    - script:
      name: "deploy production "
      code: |
        echo "deploy production"
        ls $WERCKER_REPORT_ARTIFACTS_DIR
        env
  after-steps:
    - script:
      name: "deploy production finish"
      code: |
        echo "deploy production finish"
        echo "===================: deploy_production start"
        echo $A
        echo $B
        echo "===================: deploy_production end"